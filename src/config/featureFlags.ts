// Simple feature flags for controlling visibility of non-public features
// Toggle to reveal/hide Arena Assistant entry point in the UI
export const featureFlags = {
  showArenaAssistantLink: true,
}

export type FeatureFlags = typeof featureFlags



